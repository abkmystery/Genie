This folder is populated during packaging.

Expected output for one-click builds:
- genie-local-api.exe (built from services/local-api via PyInstaller, Windows no-console)

Do not commit built binaries to the repo.

